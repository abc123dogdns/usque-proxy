---
phase: 02-surgical-closeerror-port
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - usque-bind/bind.go
autonomous: true
requirements:
  - RDLY-01
  - CERR-04
must_haves:
  truths:
    - "Reconnection uses constant 1-second delay, never exponential backoff"
    - "nextBackoff function no longer exists"
    - "QUIC config uses fixed values: KeepAlivePeriod 30s, MaxIdleTimeout 120s, InitialPacketSize 1280, DisablePathMTUDiscovery true"
    - "SetNetworkHint function and networkHint variable no longer exist"
    - "DNS connections are reset on ALL reconnects, not just network-change reconnects"
    - "NetworkType field removed from tunnelConfig struct"
    - "The app builds and produces a valid AAR"
  artifacts:
    - path: "usque-bind/bind.go"
      provides: "Simplified reconnect loop with constant delay and fixed QUIC config"
      contains: "reconnectDelay = 1 * time.Second"
  key_links:
    - from: "maintainTunnel reconnect loop"
      to: "sleepCtx"
      via: "constant reconnectDelay instead of escalating backoff"
      pattern: "reconnectDelay"
    - from: "maintainTunnel"
      to: "dns.resetConnections"
      via: "unconditional call after every tunnel loss"
      pattern: "dns\\.resetConnections"
---

<objective>
Replace exponential backoff with constant 1-second reconnect delay, fix QUIC config to match usque-android values, make DNS reset unconditional on all reconnects, and remove all dead code (nextBackoff, SetNetworkHint, networkHint, adaptive QUIC logic, NetworkType).

Purpose: Simplify the reconnect logic to match usque-android's proven pattern. Exponential backoff delays reconnection unnecessarily after MASQUE session death. Adaptive QUIC config adds complexity without proven benefit. DNS should reset on every reconnect to discard stale sockets.

Output: Simplified maintainTunnel with constant delay, fixed QUIC config, unconditional DNS reset, no dead code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-surgical-closeerror-port/02-CONTEXT.md
@.planning/phases/02-surgical-closeerror-port/02-01-SUMMARY.md

<interfaces>
<!-- Reference: usque-android/api/tunnel.go lines 160, 173, 185, 254 -->
<!-- Constant reconnectDelay passed as parameter, used in time.Sleep(reconnectDelay) -->
<!-- QUIC config from usque-android internal.DefaultQuicConfig: -->
<!--   KeepAlivePeriod: 30s, MaxIdleTimeout: 120s, InitialPacketSize: 1280, DisablePathMTUDiscovery: true -->

From usque-android/api/tunnel.go:
```go
func MaintainTunnel(ctx context.Context, ..., reconnectDelay time.Duration) {
    // ...
    time.Sleep(reconnectDelay)  // constant delay on connect failure (line 173)
    // ...
    time.Sleep(reconnectDelay)  // constant delay on non-200 response (line 185)
    // ...
    time.Sleep(reconnectDelay)  // constant delay after tunnel loss (line 254)
}
```

From usque-android internal.DefaultQuicConfig:
```go
&quic.Config{
    EnableDatagrams:         true,
    InitialPacketSize:       1280,
    KeepAlivePeriod:         30 * time.Second,
    MaxIdleTimeout:          120 * time.Second,
    DisablePathMTUDiscovery: true,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace backoff with constant delay and fix QUIC config</name>
  <files>usque-bind/bind.go</files>
  <read_first>
    - usque-bind/bind.go (read the full maintainTunnel function starting at line 387, nextBackoff at line 602, SetNetworkHint at line 209, networkHint at line 205)
    - usque-android/api/tunnel.go (lines 146-256 for reference reconnect pattern)
  </read_first>
  <action>
**In maintainTunnel function (line 387):**

1. **Replace constants block (lines 388-395)** — per D-06, D-14: Remove `packetSize = 1242`, `minBackoff`, `maxBackoff`. Add `reconnectDelay = 1 * time.Second`. Keep `mtu = 1280`, `connectPort = 443`, `certRenewBefore = 1 * time.Hour`.
   ```go
   const (
       mtu             = 1280
       connectPort     = 443
       reconnectDelay  = 1 * time.Second
       certRenewBefore = 1 * time.Hour
   )
   ```

2. **Remove networkGraceMax constant (line 443)** — per D-07: Delete `const networkGraceMax = 3`.

3. **Remove networkHint seeding (lines 446-448)** — per D-13, D-15: Delete the `if cfg.NetworkType != "" { networkHint.Store(cfg.NetworkType) }` block.

4. **Remove backoff and networkGraceAttempts variables (lines 450-451)** — per D-11, D-07: Delete `backoff := minBackoff` and `networkGraceAttempts := 0`.

5. **Replace cert error backoff (lines 466-468)** — Replace `sleepCtx(ctx, backoff)` + `backoff = nextBackoff(backoff, maxBackoff)` with just `sleepCtx(ctx, reconnectDelay)`.

6. **Replace TLS error backoff (lines 478-479)** — Same replacement: `sleepCtx(ctx, reconnectDelay)`.

7. **Remove adaptive QUIC config block (lines 486-512)** — per D-12, D-13: Delete the entire `hint` variable, switch block, `disablePMTU`, `pktSize` variables. Replace with fixed config:
   ```go
   quicCfg := &quic.Config{
       EnableDatagrams:         true,
       InitialPacketSize:       1280,
       KeepAlivePeriod:         30 * time.Second,
       MaxIdleTimeout:          120 * time.Second,
       DisablePathMTUDiscovery: true,
   }
   ```

8. **Replace connect-failure backoff (lines 521-532)** — Keep the `waitForNetwork` path but replace `backoff = minBackoff` with nothing (constant delay handles it). Replace else branch: `sleepCtx(ctx, backoff)` + `backoff = nextBackoff(backoff, maxBackoff)` with `sleepCtx(ctx, reconnectDelay)`.
   ```go
   if !hasNetwork.Load() {
       log.Println("no network - waiting for connectivity")
       waitForNetwork(ctx)
   } else {
       sleepCtx(ctx, reconnectDelay)
   }
   ```

9. **Replace tunnel-rejected backoff (lines 538-539)** — Replace with `sleepCtx(ctx, reconnectDelay)`.

10. **Remove `backoff = minBackoff` after successful connection (line 544)** — per D-11: Delete this line. No backoff state to reset.

11. **Remove isNetworkReconnect variable and simplify reconnectCh handling (lines 556-573)** — per D-08, D-16:
    - Delete `isNetworkReconnect := false`
    - In the `<-reconnectCh` case, remove the `networkTriggered` check and `backoff` assignment. Just keep:
      ```go
      case <-reconnectCh:
          connected.Store(false)
          connectedAt.Store(0)
          log.Println("reconnect requested")
      ```

12. **Make DNS reset unconditional (lines 586-588)** — per D-16: Replace `if isNetworkReconnect && dns != nil` with just `if dns != nil`:
    ```go
    if dns != nil {
        dns.resetConnections()
    }
    ```

13. **Replace end-of-loop backoff logic (lines 590-598)** — per D-06: Replace the entire `if backoff > 0` + `networkGraceAttempts` block with just:
    ```go
    sleepCtx(ctx, reconnectDelay)
    ```

**Remove nextBackoff function (lines 602-612)** — per D-10: Delete the entire function.

**Remove SetNetworkHint function (lines 207-211)** — per D-13: Delete function and its doc comment.

**Remove networkHint variable (lines 203-205)** — per D-13: Delete `var networkHint atomic.Value` and its doc comment.

**Remove NetworkType from tunnelConfig struct (line 69)** — per D-15: Delete `NetworkType string \`json:"network_type"\`` field.

**Clean up imports** — Remove `mrand "math/rand/v2"` (was only used by nextBackoff for jitter). Keep all other imports.

**Clean up Reconnect function (lines 226-237)** — The `networkTriggered` atomic is still used by `Reconnect()` and `SetConnectivity()`. Since `maintainTunnel` no longer reads `networkTriggered`, remove `networkTriggered.Store(true)` from `Reconnect()`. Also remove the `var networkTriggered atomic.Bool` declaration (line 201) and the `networkTriggered.Store(false)` in `StartTunnel` (line 170).
  </action>
  <verify>
    <automated>cd /var/home/nhubao/StudioProjects/usque-proxy/usque-bind && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` in usque-bind/ succeeds with no errors
    - grep 'reconnectDelay.*=.*1 \* time.Second' bind.go returns 1 match
    - grep 'nextBackoff' bind.go returns 0 matches (function removed)
    - grep 'func SetNetworkHint' bind.go returns 0 matches (function removed)
    - grep 'networkHint' bind.go returns 0 matches (variable removed)
    - grep 'NetworkType' bind.go returns 0 matches (struct field removed)
    - grep 'networkGraceAttempts' bind.go returns 0 matches (variable removed)
    - grep 'networkGraceMax' bind.go returns 0 matches (constant removed)
    - grep 'isNetworkReconnect' bind.go returns 0 matches (variable removed)
    - grep 'networkTriggered' bind.go returns 0 matches (atomic removed)
    - grep 'mrand' bind.go returns 0 matches (import removed)
    - grep 'math/rand' bind.go returns 0 matches (import removed)
    - grep 'packetSize' bind.go returns 0 matches (constant removed)
    - grep 'minBackoff' bind.go returns 0 matches (constant removed)
    - grep 'maxBackoff' bind.go returns 0 matches (constant removed)
    - grep 'KeepAlivePeriod.*30 \* time.Second' bind.go returns 1 match
    - grep 'MaxIdleTimeout.*120 \* time.Second' bind.go returns 1 match
    - grep 'InitialPacketSize.*1280' bind.go returns 1 match
    - grep 'DisablePathMTUDiscovery.*true' bind.go returns 1 match
    - grep 'dns.resetConnections' bind.go occurs without any isNetworkReconnect guard
  </acceptance_criteria>
  <done>maintainTunnel uses constant 1s reconnect delay. QUIC config is fixed at usque-android values. DNS resets on all reconnects. All dead code removed (nextBackoff, SetNetworkHint, networkHint, networkTriggered, adaptive QUIC, NetworkType, backoff state).</done>
</task>

<task type="auto">
  <name>Task 2: Build AAR and verify final state</name>
  <files>usque-bind/bind.go</files>
  <read_first>
    - usque-bind/bind.go (verify final state after Task 1 modifications)
    - build-usque.sh (build script to produce AAR)
  </read_first>
  <action>
1. Run `cd /var/home/nhubao/StudioProjects/usque-proxy && bash build-usque.sh` to produce `app/libs/usquebind.aar`.
2. If the build fails, diagnose and fix any compilation issues in bind.go.
3. Verify the AAR was produced: `ls -la app/libs/usquebind.aar`.
4. Run a final comprehensive check:
   - Confirm all 3 CloseError checks exist in forwarding loops (from Plan 01)
   - Confirm no backoff/adaptive/networkHint code remains
   - Confirm constant reconnectDelay is used throughout
   - Confirm DNS reset is unconditional
  </action>
  <verify>
    <automated>ls -la /var/home/nhubao/StudioProjects/usque-proxy/app/libs/usquebind.aar && cd /var/home/nhubao/StudioProjects/usque-proxy/usque-bind && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - app/libs/usquebind.aar exists and has a recent modification timestamp
    - `go vet ./...` in usque-bind/ produces no warnings
    - `go build ./...` in usque-bind/ succeeds
  </acceptance_criteria>
  <done>AAR built successfully from modified Go code. go vet passes. All phase 2 changes verified in the built artifact.</done>
</task>

</tasks>

<verification>
1. `cd usque-bind && go build ./...` compiles
2. `cd usque-bind && go vet ./...` passes
3. `grep -c 'errors.As(err, new(\*connectip.CloseError))' usque-bind/bind.go` returns 3
4. `grep -c 'nextBackoff\|networkHint\|NetworkType\|networkGraceAttempts\|isNetworkReconnect\|networkTriggered\|mrand\|packetSize.*1242\|minBackoff\|maxBackoff' usque-bind/bind.go` returns 0
5. `grep 'reconnectDelay' usque-bind/bind.go` shows constant 1s delay
6. `app/libs/usquebind.aar` exists with recent timestamp
</verification>

<success_criteria>
- Constant 1-second reconnect delay replaces exponential backoff
- Fixed QUIC config (30s keepalive, 120s idle, 1280 packet, PMTU disabled)
- DNS reset on all reconnects
- All dead code removed (nextBackoff, SetNetworkHint, networkHint, networkTriggered, adaptive QUIC, NetworkType)
- AAR builds successfully
- go vet clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-surgical-closeerror-port/02-02-SUMMARY.md`
</output>
